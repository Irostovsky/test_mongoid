<div id="<%= dom_id(comment) %>">
  <h3><%= comment.name %></h3>
  <p><%= comment.content %></p>
  <p><%= link_to 'Delete', article_comment_path(@article, comment), :method => :delete, :confirm => 'Are you sure?', :remote => true %></p>
</div>
